feat(计时/昵称): 出牌阶段窗口改用 turnTimeSeconds；玩家卡片显示登录用户名

- 出牌阶段：runAutomaticScheduler 新增 turnDeadlineAt 参数（默认等于
  responseDeadline），resolvePhaseBody 创建 playPhaseAction/discardPhaseAction
  窗口时用 turnDeadlineAt()，其余窗口（响应/判定/专属）仍用 responseDeadline；
  gameService.stabilize 传 clock()+turnTimeSeconds*1000
- 昵称：AppUser 增加可选 username；resolveSession 从 accounts 反查 username
  返回；server 连接 user 携带 username；投影 publicView.players[].nickname
  优先 username，回退 displayName/玩家N（旧持久化状态兼容）
- 测试：新增 gameTiming.test.ts 2 项（play 窗口 deadline=1060 而非 1020；
  nickname=账号1 而非 昵称1）；删除 gameViewChoices 中过时的 forfeit 测试
  （退出确认弹窗已由 gameLeaveDialogs.test.ts 覆盖）
- 服务端 523 项、客户端 90 项全过、两端构建通过

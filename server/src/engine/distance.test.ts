import {resolve} from "node:path";import {beforeAll,describe,expect,it} from "vitest";import {loadFrozenRuleset} from "../ruleset/loadRuleset.js";import type {LoadedRuleset} from "../ruleset/types.js";import {calculateBaseDistance,calculateEffectiveDistance} from "./distance.js";import {createInitialSetup} from "./setup.js";
let ruleset:LoadedRuleset;const users={1:"u1",2:"u2",3:"u3",4:"u4"} as const,characters={1:"character.knight",2:"character.alchemist",3:"character.ranger",4:"character.wizard"} as const;beforeAll(async()=>{ruleset=await loadFrozenRuleset(resolve(import.meta.dirname,"../../../rulesets/v1.3.4"));});const state=()=>createInitialSetup(ruleset,{gameId:"distance",firstSeat:1,seed:2,usersBySeat:users,characterIdsBySeat:characters});describe("distance",()=>{it("uses the shorter occupied arc",()=>{const game=state();expect(calculateBaseDistance(game,1,3)).toBe(2);game.players[1]!.lifeState="eliminated";expect(calculateBaseDistance(game,1,3)).toBe(1);});it("applies signed attack and defense modifiers with a zero floor",()=>{const game=state();game.players[0]!.limits.offenseDistanceModifier=-2;game.players[2]!.limits.defenseDistanceModifier=1;expect(calculateEffectiveDistance(game,1,3)).toBe(1);game.players[2]!.limits.defenseDistanceModifier=-9;expect(calculateEffectiveDistance(game,1,3)).toBe(0);});it("keeps the off-field traveler in the distance ring",()=>{const game=state();game.players[1]!.characterId="character.interdimensional_traveler";game.players[1]!.presence="leftPlay";expect(calculateBaseDistance(game,1,3)).toBe(2);});});

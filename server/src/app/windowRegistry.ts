import type {LoadedRuleset} from "../ruleset/types.js";
import type {AuthoritativeGameState} from "../engine/state.js";
import {BerserkerRageSession} from "../engine/berserkerRage.js";
import {C6LaserSweepSession,C6FocusedBombardmentSession} from "../engine/c6h8o6.js";
import {CriticalPenetrationSession} from "../engine/triggerAttackFollowup.js";
import {CrystalCrabActivePincerSession} from "../engine/crystalCrab.js";
import {DarkKnightFinalStrikeSession} from "../engine/darkKnightFinalStrike.js";
import {DemolitionTeamSession} from "../engine/demolitionTeam.js";
import {DivineBarrierDamageSession} from "../engine/divineBarrier.js";
import {EngineerMechChoiceSession} from "../engine/engineer.js";
import {ExtraGemDeathTransferSession} from "../engine/dying.js";
import {ForesightSession} from "../engine/foresight.js";
import {GoldenMaskTargetSession} from "../engine/goldenMask.js";
import {InternetAddictionSession} from "../engine/internetAddiction.js";
import {MinerSession} from "../engine/miner.js";
import {OwlSession} from "../engine/owl.js";
import {PurpleLordHeroBladeSession} from "../engine/purpleLord.js";
import {QiBallDismantleSession} from "../engine/qiMaster.js";
import {ReforgeFurnaceSelectionSession} from "../engine/furnace.js";
import {RedLordHammerSession} from "../engine/redLordHammer.js";
import {SheepSession} from "../engine/sheep.js";
import {SuperBabyResponseSession} from "../engine/superBaby.js";
import {TemporaryCoinSession} from "../engine/coinGun.js";
import {BombDetonationSession} from "../engine/trapMaster.js";
import {TriggerCardSelectionSession} from "../engine/triggerCardSelection.js";
import {ValkyrieResponseSession} from "../engine/valkyrie.js";
import {ParticleEagleFollowUpSession} from "../engine/attackLifecycle.js";
import {WrenchChoiceSession} from "../engine/wrenchWeapon.js";
import {WizardSpellStrikeSession} from "../engine/wizard.js";
import {StatueChoiceSession,StatueResolutionSession} from "../engine/statueEffects.js";
import {chooseWithSource} from "../engine/random.js";

type Session={state:AuthoritativeGameState;handle(command:any):any;handleTimeout?(commandId:string):any};
type Factory=(s:AuthoritativeGameState,r:LoadedRuleset,deadline:()=>number)=>Session;
const construct=(Class:new(...args:any[])=>unknown,args:any[])=>new Class(...args) as Session;
const standard=(Class:new(...args:any[])=>unknown):Factory=>(s,r,d)=>{const session=construct(Class,[s,r,d]);const genericTimeout=(commandId:string)=>{const w=s.pendingWindows[0];if(!w)throw new Error("PROMPT_CLOSED");const pass=w.legalOfferIds.find(id=>id.endsWith(":pass"))??w.legalOfferIds.find(id=>id.includes(":pass:"));let offerId=pass??w.legalOfferIds[0]!;if(!offerId)throw new Error("NO_TIMEOUT_OFFER");const pick=(candidates:string[],purpose:string):string=>{const randomSeq=s.randomSource.nextRandomSeq,chosen=chooseWithSource(candidates,s.randomSource);s.randomSource=chosen.source;s.randomHistory.push({randomSeq,purpose,candidateRefs:candidates,resultRefs:[chosen.value]});return chosen.value;};const selections:Record<string,unknown[]>={};const cardRefs=stringArray(w.context?.legalCardRefs);if(cardRefs.length)selections.cards=[pick(cardRefs,`timeout.${w.kind}.card`)];if(!pass&&w.timeoutPolicy==="randomLegal"&&w.legalOfferIds.length>1)offerId=pick([...w.legalOfferIds],`timeout.${w.kind}.offer`);const actor=s.players.find(p=>p.seat===w.prioritySeat);if(!actor)throw new Error("NO_TIMEOUT_ACTOR");const command=buildWindowCommand({commandId,gameId:s.gameId,expectedStateRevision:s.stateRevision,actorUserId:actor.userId,promptId:w.promptId,offerId,selections});try{return session.handle(command);}catch{return{accepted:false as const,commandId,stateRevision:s.stateRevision,reasonCode:"TIMEOUT_CONTEXT_INVALID",refreshRequired:true};}};return{get state(){return session.state},handle:c=>session.handle(c),handleTimeout:typeof session.handleTimeout==="function"?(id:string)=>session.handleTimeout!(id):genericTimeout};};
const stringArray=(value:unknown):string[]=>Array.isArray(value)?value.filter((item):item is string=>typeof item==="string"):[];
const seatFromTarget=(ref:string|undefined|null):number|undefined=>{if(!ref)return undefined;const match=/^character:([1-4])$/.exec(ref);return match?Number(match[1]):undefined;};
export function buildWindowCommand(input:{commandId:string;gameId:string;expectedStateRevision:number;actorUserId:string;promptId:string;offerId:string;selections:Record<string,unknown[]>}):Record<string,unknown>{
 const cards=stringArray(input.selections.cards),killCards=stringArray(input.selections.killCards),targets=stringArray(input.selections.targets),meleeTargets=stringArray(input.selections.meleeTarget),laserTargets=stringArray(input.selections.laserTarget),options=input.selections.option??input.selections.mode??input.selections.number??[],targetRef=targets[0];
 return{commandId:input.commandId,gameId:input.gameId,expectedStateRevision:input.expectedStateRevision,actorUserId:input.actorUserId,promptId:input.promptId,offerId:input.offerId,cardRefs:cards,cardRef:cards[0],killCardRefs:killCards.length?killCards:cards,killCardRef:killCards[0]??cards[0],targetRefs:targets,targetRef,targetSeat:seatFromTarget(targetRef),weaponRef:cards[0],selectedWeaponRefs:cards,selectedWeaponRef:cards[0],meleeTargetRef:meleeTargets[0],laserTargetRef:laserTargets[0],choice:options[0],mode:options[0],amount:options[0],confirm:input.selections.confirm?.[0]===true};
}
const factories:Record<string,Factory>={
 berserkerRage:standard(BerserkerRageSession),c6LaserSweepRequest:standard(C6LaserSweepSession),c6FocusedBombardmentRequest:standard(C6FocusedBombardmentSession),criticalPenetration:standard(CriticalPenetrationSession),crystalCrabActivePincer:standard(CrystalCrabActivePincerSession),darkKnightFinalStrike:standard(DarkKnightFinalStrikeSession),divineBarrierDamage:standard(DivineBarrierDamageSession),engineerMechChoice:standard(EngineerMechChoiceSession),extraGemDeathTransfer:standard(ExtraGemDeathTransferSession),extraGemDyingResult:standard(ExtraGemDeathTransferSession),foresightDrawChoice:standard(ForesightSession),goldenMaskTarget:standard(GoldenMaskTargetSession),internetAddictionDodgeRequest:standard(InternetAddictionSession),minerDigAtPlayEnd:standard(MinerSession),minerNaturalExitTarget:standard(MinerSession),minerSourceDismantle:standard(MinerSession),owlCounterattack:standard(OwlSession),purpleLordHeroBlade:standard(PurpleLordHeroBladeSession),qiBallDismantle:standard(QiBallDismantleSession),reforgeFurnaceSelection:standard(ReforgeFurnaceSelectionSession),sheepPhaseOneDodgeRequest:standard(SheepSession),superBabyDodgeRequest:standard(SuperBabyResponseSession),temporaryCoinImmediateUse:standard(TemporaryCoinSession),trapBombDetonation:standard(BombDetonationSession),triggerCardSelection:standard(TriggerCardSelectionSession),valkyrieBossResponse:standard(ValkyrieResponseSession),weaponParticleEagleFollowUp:standard(ParticleEagleFollowUpSession),weaponW61Choice:standard(WrenchChoiceSession),wizardSpellStrike:standard(WizardSpellStrikeSession),redLordSealingHammer:standard(RedLordHammerSession),statueCardSelection:(s,r,d)=>{const x=new StatueChoiceSession(s,r);return{get state(){return x.state},handle:c=>x.handle(c),handleTimeout:(id)=>{const w=x.state.pendingWindows[0];if(!w)throw new Error("PROMPT_CLOSED");const pass=w.legalOfferIds.find(v=>v.endsWith(":pass"));let offerId=pass??w.legalOfferIds[0]!;if(!pass&&w.timeoutPolicy==="randomLegal"&&w.legalOfferIds.length){const candidates=[...w.legalOfferIds],randomSeq=x.state.randomSource.nextRandomSeq,chosen=chooseWithSource(candidates,x.state.randomSource);x.state.randomSource=chosen.source;x.state.randomHistory.push({randomSeq,purpose:`timeout.${w.kind}`,candidateRefs:candidates,resultRefs:[chosen.value]});offerId=chosen.value;}const actor=x.state.players.find(p=>p.seat===w.prioritySeat)!;return x.handle({commandId:id,gameId:x.state.gameId,expectedStateRevision:x.state.stateRevision,actorUserId:actor.userId,promptId:w.promptId,offerId});}}},statueResolutionChoice:(s,r,d)=>{const x=new StatueResolutionSession(s,r);const execute=(command:any)=>{const w=s.pendingWindows.find(v=>v.promptId===command.promptId);if(!w)return{accepted:false as const,commandId:command.commandId,stateRevision:s.stateRevision,reasonCode:"PROMPT_CLOSED",refreshRequired:true};const offers=Array.isArray(w.context?.offers)?(w.context.offers as Array<Record<string,unknown>>):[];const targetRef=typeof command.targetRef==="string"?command.targetRef:typeof command.targetRefs?.[0]==="string"?command.targetRefs[0]:undefined;const normalized=typeof targetRef==="string"&&targetRef.startsWith("public:seat_")?`character:${targetRef.split("_")[1]}`:targetRef;const offer=offers.find(o=>o.targetRef===normalized)??offers[0];if(!offer)return{accepted:false as const,commandId:command.commandId,stateRevision:s.stateRevision,reasonCode:"OFFER_EXPIRED",refreshRequired:true};return x.handle({commandId:command.commandId,gameId:s.gameId,expectedStateRevision:s.stateRevision,actorUserId:command.actorUserId,offerId:String(offer.offerId),statueRef:String(offer.statueRef??w.context?.statueRef??""),...(typeof offer.targetRef==="string"?{targetRef:offer.targetRef}:{}),...(typeof offer.modeId==="string"?{modeId:offer.modeId}:{})},d());};return{get state(){return x.state},handle:c=>execute(c),handleTimeout:commandId=>{const w=s.pendingWindows.find(v=>v.kind==="statueResolutionChoice");if(!w)throw new Error("PROMPT_CLOSED");const actor=s.players.find(p=>p.seat===w.prioritySeat);if(!actor)throw new Error("NO_TIMEOUT_ACTOR");const targets=Array.isArray(w.context?.legalTargetRefs)?(w.context.legalTargetRefs as string[]):[];let targetRef:unknown=targets[0];if(w.timeoutPolicy==="randomLegal"&&targets.length>1){const candidates=[...targets],randomSeq=s.randomSource.nextRandomSeq,chosen=chooseWithSource(candidates,s.randomSource);s.randomSource=chosen.source;s.randomHistory.push({randomSeq,purpose:`timeout.${w.kind}`,candidateRefs:candidates,resultRefs:[chosen.value]});targetRef=chosen.value;}return execute({commandId,gameId:s.gameId,expectedStateRevision:s.stateRevision,actorUserId:actor.userId,promptId:w.promptId,targetRef});}};},statuePaladinResponse:standard(StatueChoiceSession),statuePriestTake:standard(StatueChoiceSession),statueKnightDuel:standard(StatueChoiceSession),statueKnightWeapon:standard(StatueChoiceSession),
 demolitionOptionalDiscard:(s,r)=>{const x=new DemolitionTeamSession(s,r);return{get state(){return x.state},handle:c=>x.handleChoice(c),handleTimeout:id=>x.handleTimeout(id)}},demolitionWeaponOverflow:(s,r)=>{const x=new DemolitionTeamSession(s,r);return{get state(){return x.state},handle:c=>x.handleChoice(c),handleTimeout:id=>x.handleTimeout(id)}},
};
export function supportsWindow(kind:string){return kind in factories;}
export function executeWindow(state:AuthoritativeGameState,ruleset:LoadedRuleset,userId:string,deadline:()=>number,input:{commandId:string;gameId:string;expectedStateRevision:number;promptId:string;offerId:string;selections:Record<string,unknown[]>}){const factory=factories[state.pendingWindows.find(x=>x.promptId===input.promptId)?.kind??""];if(!factory)return null;const session=factory(state,ruleset,deadline),command=buildWindowCommand({...input,actorUserId:userId});return{session,result:session.handle(command)};}
export function timeoutWindow(state:AuthoritativeGameState,ruleset:LoadedRuleset,deadline:()=>number,commandId:string){const window=state.pendingWindows[0],factory=window?factories[window.kind]:undefined;if(!factory)return null;const session=factory(state,ruleset,deadline);if(!session.handleTimeout)return null;return{session,result:session.handleTimeout(commandId)};}

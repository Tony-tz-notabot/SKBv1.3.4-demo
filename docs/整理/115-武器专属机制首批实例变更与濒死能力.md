# 武器专属机制首批：实例变更与濒死能力

状态：2026-08-02 完成。

## 已完成

- W30一拳：仅在`charge_3`攻击通过杀无效检查并正式提交后，同一卡牌实例转为W39；提前无效不转形。
- W39一拳（白）：在装备区域之间移动保持形态；离开装备区时恢复同实例的W30基础模板，并清除转形运行时标记。
- W50玄冥：公开预选方式一解析当前火/冰实例状态；方式一正式提交后由冻结`toggle`映射切换，方式二不切换。
- W43重生十字章：无需预选；装备有效且本人处于濒死救援时，仅在响应权轮到本人时提供可选报价。发动后回复至血量上限、立即解除濒死并失去该牌；装备失效时不报价。

## 实现边界

W30与W50共用`weaponCommitEffects.ts`解释冻结`onAttackCommit`，支持同实例`transformCard`及实例状态`setValue/toggle`，不按中文牌名分支。转形离场恢复由通用区域移动原语执行。W43复用统一濒死优先权、幂等命令、区域移动和外围攻击恢复流程。

## 验证

直接测试覆盖正式提交时机、元素切换、离开装备恢复、W43本人响应和装备失效。服务端全量验证目标为90个测试文件、366项测试及TypeScript构建全部通过。
